import 'package:flutter/material.dart';

class CompassWithQibla extends StatelessWidget {
  const CompassWithQibla({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
